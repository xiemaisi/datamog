# order-demo.dl -- instantiate order.dl, taking its default `lt`.
cover(1, 2). cover(1, 3). cover(2, 4). cover(3, 4).

input predicate bottom(x: integer) := minimal from "order.dl"(cover = cover).

?- bottom(X).
